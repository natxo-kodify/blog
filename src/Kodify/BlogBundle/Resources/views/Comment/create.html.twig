{% extends 'KodifyBlogBundle::base.html.twig' %}

{% block content %}

    <form action={{path('create_comment',{postid:postid})}} method="post" {{ form_enctype(form) }}>

        {{ form_errors(form) }}

        {{ form_rest(form) }}
    
    </form>

{% endblock %}
